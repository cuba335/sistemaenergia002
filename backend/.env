PORT=3001
DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=sistema_energia_eolica

JWT_SECRET=un_secreto_fuerte
JWT_EXPIRES=4h

# URL base de tu frontend para construir el enlace del reset
APP_BASE_URL=http://localhost:3000

# Si quieres email real, configura SMTP (opcional).
# Si no lo configuras, el backend imprimirá el enlace de reset en la consola.
# SMTP_HOST=smtp.tu-proveedor.com
# SMTP_PORT=587
# SMTP_USER=tu_usuario
# SMTP_PASS=tu_password
